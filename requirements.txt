# Core dependencies (already required)
playwright>=1.40.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# LLM Task Planner dependencies
openai>=1.0.0  # For OpenAI GPT models
anthropic>=0.18.0  # For Anthropic Claude models
requests>=2.31.0  # For local models (Ollama, etc.)

# Optional but recommended
python-dotenv>=1.0.0  # For managing API keys in .env files
